# 活跃任务跟踪

## 任务 1：Rosetta FlexPepDock — WT GHRH(1-29) vs DPP-IV
**状态**：🔄 运行中（16 进程并行）
**阻塞项**：无
**下一步**：监控完成（预计 ~4 小时），提取最优构象
**预计完成时间**：~4 小时
**备注**：首次尝试因种子参数错误（`-seed` → `-constant_seed` + `-jran`）失败，已修正重跑。当前各 worker 已完成 5-6 个模型。

---

## 任务 2：Rosetta FlexPepDock — D-Ala2 突变体 vs DPP-IV
**状态**：⏸️ 暂停（等 WT 完成后再评估是否执行）
**阻塞项**：等 WT 对接完成
**下一步**：如需执行，将 PDB 中 `ALA` 改为 `DAL`，启动并行对接
**预计完成时间**：~4 小时（如执行）

---

## 任务 3：GROMACS MD — WT 复合物（200 ns）
**状态**：⏳ 等待中（需 Task 1 完成）
**阻塞项**：需最优对接构象
**下一步**：
  1. 用最优 FlexPepDock 模型构建 GROMACS 体系
  2. pdb2gmx → 溶剂化 → 加离子 → 能量最小化 → NVT 平衡 → NPT 平衡 → 生产模拟
  3. GPU 加速运行（RTX 3090）
**预计完成时间**：24–36 小时

---

## 任务 4：GROMACS MD — D-Ala2 突变体（200 ns）
**状态**：⏳ 就绪（拓扑已解决）
**阻塞项**：需 Task 1/2 的最优对接构象
**下一步**：
  1. 将最优构象中的 `ALA` 重命名为 `DALA`
  2. GROMACS 体系构建（pdb2gmx 已验证通过）
  3. 运行 200 ns MD
**预计完成时间**：24–36 小时
**备注**：amber14sb 的 hdb 文件存在上游损坏，已用 amber19sb 的干净版本替换，并添加了 DALA RTP 条目，pdb2gmx 验证通过。

---

## 任务 5：Step 4 数据分析与可视化
**状态**：⏳ 等待中
**阻塞项**：需 Task 1、3、4 完成
**下一步**：
  - RMSF、SASA 分析
  - 关键距离/角度时序（Ser630-Ala2、N-term-Glu205/206）
  - 氢键占有率统计
  - MM-PBSA 结合自由能
  - WT vs D-Ala2 对比图表
**预计完成时间**：1 天

---

## 任务 6：三肽几何对比可视化
**状态**：📋 待执行
**阻塞项**：无
**下一步**：用 PyMOL 生成 WT YAD 与 D-Ala2 YAD 在 DPP-IV 活性口袋中的叠加对比图
**预计完成时间**：30 分钟

---

*最后更新：2026-05-13*
*维护者：Kimi Code CLI*
